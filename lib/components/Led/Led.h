#if !defined(LED_OFF) || !defined(LED_ON) || !defined(BLINKING_SLOW) || !defined(BLINKING_FAST)
  #define LED_OFF         0
  #define LED_ON          1
  #define LED_BLINKING_SLOW   2
  #define LED_BLINKING_FAST   3
#endif
